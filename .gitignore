# R session / history
.Rproj.user/
.Rhistory
.RData
.Ruserdata
.Rapp.history

# R package build artifacts
*.tar.gz
*.Rcheck/

# OS / editor
.DS_Store
Thumbs.db

# Claude
.claude/

# Dados (parquet) ficam fora do versionamento: o pacote ja exclui data/ via
# .Rbuildignore, e a app usa um diretorio gravavel proprio (R_user_dir) em
# producao. Os arquivos permanecem em disco localmente, apenas nao sao mais
# rastreados pelo git.
data/
